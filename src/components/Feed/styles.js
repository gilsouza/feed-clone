import styled from 'styled-components';

export const Container = styled.section`
    height: 100%;
    overflow: hidden;
    width: min(600px, 100%);

    > div {
        width: min(600px, 100%);
    }
`;
